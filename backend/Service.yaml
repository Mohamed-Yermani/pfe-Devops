apiVersion: v1
kind: Service
metadata:
  name: cnss-back
  namespace: cnss
spec:
  type: NodePort
  selector:
    app: cnss-back
  ports:
    - port: 8094
      targetPort: 8094
      nodePort: 30001
      protocol: TCP
