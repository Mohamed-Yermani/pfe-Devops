apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cnss-db-pvc
  namespace: cnss
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: local-path
